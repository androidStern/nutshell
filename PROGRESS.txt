Nutshell release state, current as of 2026-06-10 (v0.1.24 published):

v0.1.24 (current): fixes the two product bugs the v0.1.23 VM gates caught.
- Public v0.1.24 from main 7b4f903. Release: https://github.com/androidStern/nutshell/releases/tag/v0.1.24
- Tarball SHA256 206e7e31caddd9332bca5487cbac65550d0e4f822a28605e331542db4e3951b5; tap commit 8b35302; SHAs verified matching.
- Product fixes: (1) signed-out X ("auth cookies missing") now classifies twitter_signed_out/needs_auth, not session_check_failed/blocked_bug; (2) Notes AppleEvent -1712 consent-timeout now classifies apple_notes_automation_permission_required/needs_permission. Both have regression tests.
- Harness fixes: permissions-pre contract encodes macOS reality (FDA root cause + notes needs_permission + podcasts needs_permission-or-no-library; Chrome cookie store is NOT FDA-protected so browser sources may pass pre-grant); signed-out gate accepts only *_signed_out codes with needs_auth guidance. Replay-verified against frozen v0.1.23 evidence.
- Local certification (246 tests) + certify:release passed before publish.
- Gate status v0.1.24: local import gate PASS (reports/local-provider-imports-v0.1.24-20260610.json). VM gates re-run 2026-06-10 from fresh clones, all PASS on public Homebrew 0.1.24:
  - Signed-out gate PASS: ~/Documents/NutshellRehearsalShare/reports/signedout-gate-v0.1.24-20260610b.json (clean-state + installed-product + unauthenticated-browser-state, all verdict pass). Fix (1) confirmed at field level: twitter now emits twitter/twitter_signed_out with guidance {state: needs_auth, fix: "Open x.com in Chrome and sign in, then retry.", confirm: "nutshell doctor twitter"}; youtube/youtube_signed_out needs_auth as before. Evidence: signedout-gate-v0.1.24-20260610b-evidence-doctor-{youtube,twitter}.json + -evidence-version.txt.
  - Signed-in gate PASS: reports/signedin-gate-v0.1.24-20260610b.json (installed-product + authenticated-browser-state). Fixture preflight healthy (Google SAPISID/__Secure-1PSID et al., X auth_token/ct0, decryptable, zero warnings); both doctors usable post-login (ready_empty, only backfill_incomplete); no Safe Storage/Keychain prompts.
  - Permissions-pre gate PASS under the realistic contract: reports/permissions-pre-gate-v0.1.24-20260610b.json. FDA root cause reported (system/nutshell_app_full_disk_access_missing needs_permission); fix (2) confirmed: apple_notes emits apple_notes/apple_notes_automation_permission_required with guidance {state: needs_permission, fix: "Click Allow on the macOS automation prompt, or enable Nutshell under System Settings → Privacy & Security → Automation → Notes, then retry.", confirm: "nutshell doctor apple_notes"}; podcasts honest empty library (backfill_incomplete/ready_empty); youtube/twitter honestly classified pre-grant (backfill_incomplete only, no fake-ready, no false needs_permission). Evidence: permissions-pre-gate-v0.1.24-20260610b-evidence-doctor-all.json + -evidence-version.txt.
  - Each VM printed nutshell 0.1.24 before verifiers ran; doctor --json carries finding.guidance everywhere. Clones nutshell-gate-{signedout,signedin,permpre}-v0124 stopped and deleted; base snapshots untouched.
- Post-permission snapshot exists as of 2026-06-10: nutshell-postperm-sequoia-20260610 (human session per docs/post-permission-snapshot-session.md; public v0.1.24, FDA + Keychain Always-Allow + Notes automation granted to Nutshell.app, agent enabled, smoke sync ingested 4373 records BEFORE the three "Nutshell rehearsal note one/two/three" notes were created). PERMANENT: never boot/modify directly, only clone. Exit verification PASS from a headless clone: zero needs_permission findings, FDA granted, nutshell 0.1.24 — evidence reports/postperm-snapshot-verify-20260610-{doctor,health}.json (health.status is "warning" from standing backfill findings; that is the by-design no-imports state, recorded for the livesync contract discussion below).
- Permissions-post gate PASS: reports/permissions-post-gate-v0.1.24-20260610.json (mode post, verdict pass). doctor JSON exit 1 (warnings only), zero needs_permission among 6 findings (3x backfill_incomplete, twitter backfill_partial/last_run_partial/twitter_enrichment_pending), app status Full Disk Access granted to /Users/admin/Applications/Nutshell.app, seeded notes visible through the app: sync apple_notes uniqueNotes=3 (>=3), exit 0. Agent RunAtLoad boot sync was waited out (run.lock poll) before the verifier ran — harness arrangement, no retry needed.
- Live-sync/dashboard gate HARNESS_FAIL (frozen): reports/livesync-gate-v0.1.24-20260610.harness-failed-frozen.json. The PRODUCT numbers are good — fixture preflight healthy; live sync committed youtube insertedRecords=30, twitter=4341, apple_notes=6; podcasts seed synced through the normal plugin path (status ok); dashboard serves 0.1.24 with youtube 30 / twitter 17 / apple_notes 3 in the 7-day window. Exactly two checks failed, both gate-contract bugs, not product misbehavior (in-report verdict says product_fail only because that is the machinery default for fail):
  (1) "final health is ok with background sync enabled" failed solely on health_warning. App block is perfect (FDA granted, backgroundSync enabled, agent enabled). The warnings are the by-design standing findings on a no-imports fixture: 3x backfill_incomplete + twitter backfill_partial (ready_empty guidance, "import your provider export") and twitter last_run_partial/twitter_enrichment_pending (enrichment backlog draining under rate-limit backoff after the 4341-record ingest). The livesync gate deliberately runs WITHOUT archive imports (gates doc: imports must not stand in for live records), so health.status=="ok" (zero warnings, src/health/health.ts reportStatus) is unsatisfiable by design. liveHealthCheck should accept warnings whose codes are the standing backfill/enrichment set (or assert "no criticals + app block correct") — same contract-vs-reality class as the v0.1.24 permissions-pre fix.
  (2) "dashboard shows podcasts trace records" failed with count 0. The harness queries /api/days with NO params, and dashboardDays defaults from = to minus 7 days (src/dashboard/server.ts:139-150). The SQLite-safe podcasts seed is frozen 2026-05-29 — its newest podcast.listened is 12 days old, so NO seed older than 7 days can ever appear in that window. The product honestly synced the seed (status ok) and honestly shows zero in-window podcasts records. Fix options: assert podcasts via all-time record counts (like verify-final's record stats) or query /api/days with an explicit from covering the seed manifest's snapshot date; refreshing the seed within 7 days would mask only this check, not (1).
  Arrangement that worked and is recorded in the frozen report (stage-podcast-seed + livesync-fixture-prep phases): waited out the RunAtLoad agent boot sync via run.lock poll, staged seeds/MTLibrary.sqlite through the harness (provenance manifest verified), deleted the CLONE's nutshell.sqlite(+wal/shm) and projections so the smoke-synced store would not dedupe the gate's foreground sync to zero, kept background sync ENABLED throughout (StartInterval 900s quiet window instead of disable/enable, because enable-sync re-fires RunAtLoad and liveHealthCheck requires backgroundSync enabled). Evidence: livesync-gate-v0.1.24-20260610-evidence-{health,doctor-all}.json + -evidence-version.txt.
  Next runner: fix the two liveHealthCheck/dashboard contract points in src/release/fresh-install-rehearsal.ts (replay against the frozen report first), then rerun the livesync gate from a fresh clone of nutshell-postperm-sequoia-20260610 with the same arrangement (report letter b). The candidate is NOT implicated.
- Live-sync gate RERUN (letter b, harness contract b2eb5a5) HARNESS_FAIL again (frozen): reports/livesync-gate-v0.1.24-20260610b.harness-failed-frozen.json. Progress: the standing-warning health contract now PASSES ("final health has no critical findings, only standing warnings, and a green app block": status warning with exactly youtube/podcasts/apple_notes backfill_incomplete + twitter backfill_partial/last_run_partial/twitter_enrichment_pending; FDA granted, backgroundSync enabled, agent enabled). 13 of 14 checks pass; same product numbers (youtube 30 / twitter 4341 / apple_notes 6 insertedRecords; dashboard 0.1.24, dayCount 9, youtube 30 / twitter 19 / apple_notes 3 in the 60-day window, daysRequestPath /api/days?from=2026-04-11). The ONLY failure is still "dashboard shows podcasts trace records" count 0 — and the 60-day window fix exposed the real layer: liveCommits.podcasts=0, i.e. the gate sync never INGESTS the seed. Root cause chain, all verified: `nutshell sync` defaults to --mode recent (src/cli.ts:198); the podcasts plugin recent window is request.window ?? overlapWindow(overlapHours) with overlapHours default 48 (src/plugins/builtin/podcasts/plugin.ts:96-97, src/config/defaults.ts:67/75, src/core/time.ts:42); the seed's newest play is 2026-05-28 07:52:50 (sqlite MAX(ZLASTDATEPLAYED), 13 days old). Diagnostic `nutshell sync podcasts --json` in the clone: status ok, rows 0, emitted 0, insertedRecords 0, dbPath = the staged group-container path — the plugin reads the seed fine; a frozen seed older than 48h simply has nothing in a recent window. No dashboard window can show records that were never ingested. NOT a product bug (honest behavior end to end), NOT fixture rot in the cookie sense — the gate contract must decide how podcasts seed records enter the store: (a) gate runs a bounded `nutshell sync podcasts --mode backfill --json` after the foreground sync and asserts dashboard podcasts nonzero from that (seed IS the local-OS data source; distinct from provider-archive imports), or (b) drop the dashboard-podcasts-nonzero assertion in favor of the existing "podcasts seed syncs through the normal plugin path" + a store/backfill-mode record-count assertion, or (c) require a <48h-fresh seed per run (operationally bad). Evidence: livesync-gate-v0.1.24-20260610b-evidence-{health,sync-podcasts}.json + -evidence-version.txt. Clone nutshell-gate-livesync-v0124b deleted; only the three permanent snapshots remain. Rerun after the contract decision = report letter c.
- Clones postperm-verify-tmp, nutshell-gate-permpost-v0124, nutshell-gate-livesync-v0124 stopped and deleted. VMs remaining: the two base snapshots + nutshell-postperm-sequoia-20260610 only. docs/release-validation-gates.md updated with the current post-permission snapshot name.
- Layer-4 final one-pass rehearsal still QUEUED (docs/final-release-rehearsal-checklist.md); livesync gate queued behind the harness contract fix above.

--- v0.1.23 history below (superseded by v0.1.24) ---
Nutshell release state, current as of 2026-06-10 (v0.1.23 published):

Release:
- Public v0.1.23 published from main d5c223a ("honest setup" release; branch honest-setup merged ff).
- GitHub release: https://github.com/androidStern/nutshell/releases/tag/v0.1.23
- Tarball SHA256 2e8e24178e4b00f411296504e514d606aaef8742b921e73a5493b3ddc192ad20; Homebrew tap commit 2073ae5. Formula/tag/asset SHAs verified matching.
- npm/Bun registry path is DEFERRED by explicit decision (not published, README + release contract updated). Supported installs: Homebrew tap, tarball.
- Local certification before publish: typecheck, 238 tests (includes pty golden journeys driving the real interactive setup), lint, build, build:compile, build:macos-app, build:tarball, certify:release — all passed.

What v0.1.23 is (goal: ../setup-onboarding-and-feedback-loops-goal.md, approved 2026-06-10):
- Setup verifies every source with its real probe through the Nutshell.app identity (hidden __probe bridge); ready requires a passing probe; probe/retry/skip loop; permission step BEFORE verification; re-run opens with a probed status table and fixes only what fails; real bounded smoke sync (fictional "handed off" wording removed); already-imported archives detected from store-derived backfill status.
- Every problem finding carries guidance {state, fix, confirm, url?} authored in per-plugin catalogs (TracePlugin.findings) + system/setup catalogs; universal invariant test renders every emittable code through every surface and bans raw finding construction outside catalog modules. Catch-all auth codes split one-code-one-state (keychain-blocked = needs_permission, never needs_auth).
- Scheduler self-heals: degraded source gets one bounded probe per scheduled run; pass flips ready + syncs; fail refreshes one deduped finding; *_rate_limited codes back off (catalog naming contract). Sync failing on needs_auth/needs_permission re-degrades the source.
- Doctor: root-cause-first text (FDA/app criticals lead, needs_permission symptoms collapse), aliases x/notes/podcast, no-arg works; sync prints human text with skipped-source fixes; health carries standing backfill-pending line with exact import command; dashboard renders fix text (guidance persisted in store via guidance_json column + migration v2).
- local_os sources (Notes/Podcasts) get REAL probes when health/doctor run inside the app identity (NUTSHELL_BUNDLE env check) — previously never probed.

Gate status for v0.1.23 (split gates, docs/release-validation-gates.md; verdicts pass/product_fail/harness_fail/fixture_stale):
- Local import gate: PASS. Report ~/Documents/NutshellRehearsalShare/reports/local-provider-imports-v0.1.23-20260610.json. youtube.watched 68296 + searched 6128; twitter.tweet 33394 (+bookmarks/likes); canonical types verified.
- Signed-out gate: PRODUCT_FAIL (frozen). Report ~/Documents/NutshellRehearsalShare/reports/signedout-gate-v0.1.23-20260610.product-fail-frozen.json (phases clean-state/installed-product/unauthenticated-browser-state; in-report verdicts say pass because the harness still accepts legacy TWITTER_AUTH_PROBE_CODES, but the contract assertion fails). YouTube is correct: youtube/youtube_signed_out with guidance.state needs_auth. Twitter is NOT: a signed-out user (no X cookies) gets twitter/twitter_session_check_failed with guidance.state blocked_bug and fix "Retry shortly — this is usually a temporary X or network failure". Root cause in source: src/plugins/builtin/twitter/bird-client.ts buildClient() throws "X auth cookies missing; warnings=..." and looksLikeAuthFailure() (/unauthorized|forbidden|login required|invalid.*cookie|auth.*failed/i in src/plugins/builtin/twitter/rate-limit.ts) does not match the product's own cookies-missing message, so plugin.ts check() routes to twitter_session_check_failed instead of twitter_signed_out. Evidence: signedout-gate-v0.1.23-20260610-evidence-doctor-{youtube,twitter}.json in the share. Fix code separately, publish new artifact, rerun gate from clean clone (next report letter: b). Secondary note: the harness check "twitter signed-out state is explicit" should stop accepting twitter_session_check_failed (and arguably assert guidance.state directly).
- Signed-in gate (with fixture preflight): PASS on public Homebrew 0.1.23. Report ~/Documents/NutshellRehearsalShare/reports/signedin-gate-v0.1.23-20260610.json (installed-product + authenticated-browser-state, all verdict pass). Fixture preflight healthy: Google SAPISID/APISID/SSID/HSID/__Secure-1PSID/__Secure-3PSID/SID and X auth_token/ct0 present and decryptable, zero browser warnings. Both doctors usable after login (youtubeState/twitterState ready_empty; only backfill_incomplete at source level); no Chrome Safe Storage/Keychain prompts or timeouts.
- Permissions gate PRE mode: PRODUCT_FAIL (frozen, harness-assigned verdict). Report ~/Documents/NutshellRehearsalShare/reports/permissions-pre-gate-v0.1.23-20260610.product-fail-frozen.json. The FDA root cause IS reported (system/nutshell_app_full_disk_access_missing needs_permission, plus agent/background-sync needs_permission), and no source fake-readies, but NONE of the four sources emits a source-level needs_permission finding before grants: youtube/podcasts/apple_notes/twitter each show only backfill_incomplete (ready_empty); apple_notes additionally emits apple_notes_access_failed with guidance.state blocked_bug ("AppleEvent timed out (-1712)") where the no-grants contract expects needs_permission with Notes-automation fix text. Evidence: permissions-pre-gate-v0.1.23-20260610-evidence-doctor-{all,youtube,twitter}.json in the share. POST mode still QUEUED on the human post-permission snapshot.
- Spot-check: installed 0.1.23 doctor --json carries finding.guidance {state, fix, confirm} on every problem finding in all three VMs (new guidance contract is live in the published artifact).
- All three gate clones (nutshell-gate-signedout-v0123, nutshell-gate-signedin-v0123, nutshell-gate-permpre-v0123) stopped and deleted; base snapshots untouched.
- QUEUED (need the one-time ~20min human session producing the post-permission snapshot, checklist: docs/post-permission-snapshot-session.md): permissions-post gate, live-sync/dashboard gate (rehearse:verify-permissions --mode post, rehearse:verify-livesync).
- QUEUED: final one-pass fresh-install rehearsal + fresh-macOS-user + reboot persistence = layer-4 human pass, checklist docs/final-release-rehearsal-checklist.md.
- Release is PUBLISHED but NOT validated: two product_fail gates (signed-out twitter misrouting, permissions-pre missing source-level needs_permission) require code fixes and a new published artifact, then gate restarts from clean clones. Do not claim final validation.

New operational tooling:
- scripts/snapshot-keepalive.sh refreshes the auth-present snapshot's sessions (clone, open provider pages, verify cookies decryptable, promote dated clone; fixture-stale exit 75, harness fail 70). Suggested weekly. Assumptions about tart exec/GUI session in the script header need one live verification run.
- rehearse:verify-permissions (--mode pre|post) and rehearse:verify-livesync gate runners exist with three-way verdict classification; fixture preflight queues (not fails) stale-fixture gates; rehearse:audit-report refuses validation while a required gate is queued.
- Stable snapshots: nutshell-authpresent-sequoia-google-x-20260610 (cookies fresh 2026-06-10), nutshell-strict-sequoia-base-20260609. Old strict-attempt VMs deleted 2026-06-10 (authorized).

Keep private inputs/reports/auth profiles and this PROGRESS.txt uncommitted.
Test traceability: docs/test-traceability.md (every test anchored to a criterion; gaps listed feed remaining gate work).
